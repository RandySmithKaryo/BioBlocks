using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// this class also currently controls the camera, since that's pretty much the only thing that input does

public class InputManager : MonoBehaviour
{

    // no mouse = track up down left right

    // left mouse - push in and out  (up and down) track left right

    // right mouse - pan left right, tilt up down

    private void Update()
    {
        
    }
}
